# Vision regression fixtures

These fixtures are minimized map crops used to prevent regressions in coastal
reef, airport/plaza alignment, and peninsula geometry. They contain no user
names, avatars, account screens, console identifiers, or full audit records.

The image files are interoperability evidence and are excluded from the source
code's MIT license as described in the repository `NOTICE.md`.
